---
- name: Create Jenkins Agent Node
  hosts: localhost
  gather_facts: no
  vars:
    jenkins_url: "http://localhost:8080"
    admin_username: "admin"
    secret_username: "{{ lookup('hashi_vault', 'secret=secret/data/jenkins field=linux-username') }}"
    secret_username: "{{ lookup('hashi_vault', 'secret=secret/data/jenkins field=linux-password') }}"
    api_token: "{{ lookup('hashi_vault', 'secret=secret/data/jenkins field=jenkins-api-token') }}"
    slave_config_file: "requirements.json"
    - name: Create Jenkins agent node
      ansible.builtin.uri:
        url: "{{ jenkins_url }}/computer/doCreateItem"
        method: POST
        headers:
          Content-Type: "application/x-www-form-urlencoded"
          Authorization: "Basic {{ (admin_username + ':' + api_token) | b64encode }}"
        body_format: form-urlencoded
        body:
          name: "ansible-agent2"
          type: "hudson.slaves.DumbSlave$DescriptorImpl"
          json: "{{ lookup('file', slave_config_file) }}"
        follow_redirects: true
      register: result

    - debug:
        var: result
