pipeline {
  agent any
  stages {
    stage('Hello') {
      steps {
        sh '''
          /opt/homebrew/bin/ansible --version
          /opt/homebrew/bin/ansible-playbook --version
          /opt/homebrew/bin/ansible-galaxy --version
        '''
      }
    }
  }
}
